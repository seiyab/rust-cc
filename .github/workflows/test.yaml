name: integration-test
on: [pull_request]
jobs:
  integration-test:
    name: Integration test
    runs-on: ubuntu-latest
    container: seiyab/rust-cc:latest
    steps:
      - name: test
        run: make test
